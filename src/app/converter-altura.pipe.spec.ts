import { ConverterAlturaPipe } from './converter-altura.pipe';

describe('ConverterAlturaPipe', () => {
  it('create an instance', () => {
    const pipe = new ConverterAlturaPipe();
    expect(pipe).toBeTruthy();
  });
});
